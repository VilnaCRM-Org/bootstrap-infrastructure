# Trusted controller installation candidate

This local candidate is based on main `d3015695def1c704b1265db347ed411690314b32`. Its reviewed transplant source is `3ed47332f48e7faca5e2cb8bdcc1b7be5de3ab6c`, supplemented by the parent-accepted backup read-policy repair and independently reviewed saved-plan encryption continuity closure. Exact candidate file hashes and validation are recorded in the external installation manifest. It is not an approval, a deployment receipt, a full PR192 acceptance result, or permission to change repository rules.

## Scope and dependency closure

The trusted comment intake, immutable request artifact, current-permission/head rechecks, account/environment validation, fixed scope routing, dedicated governance workflow, and App-issued promotion proof ship together. Their shared helpers and negative tests remain included. Main-only workflow trust and exact immutable GitHub repository/owner claims remain enforced by the reviewed IAM libraries.

The platform checkout consumes operator-owned controls and replication IAM. It retains purpose-specific immutable boundaries, explicit state/KMS isolation guards, scoped workload identity policies, reviewed complete-document policy pins, and saved-plan-only deployment behavior. Test state is `state/test`; prod state is `state/prod`. No automatic lock cancellation, unsaved fallback, root-prefix write allowance, or resource ownership recovery is added.

The existing alert-triage job receives only the matching CI-reader credential cutover. Its queue handler remains byte-for-byte the main version, with a hash regression. New alert fingerprinting/backfill/reconcile features remain deferred.

`infra.iam.account.assert_bootstrap_account` is extracted without a body change from the reviewed governance library, so the platform can enforce its account before resource allocation without importing the deferred governance implementation. Shared CI and operator libraries remain for coherent bounded-IAM tests and reproducibility. The independently reviewed operator project and governed resource implementation are not part of this installation checkout; retain their exact reviewed source separately. This checkout must never be substituted as an operator deployment program.

No service catalog, service scaffold, real governed resource implementation, new alert operational workflow, or revised historical approval evidence is included merely to make the split pass. Existing main operational evidence stays historical; the live freshness gates remain active.

## Acceptance and deployment-record dependencies

The user's condition still requires BMAD FR/NFR PASS, manual QA PASS, and real same-head PR-comment-triggered test and prod AWS deployments before Kravalg approval. Operator executions cannot substitute for those comment deployments. Necessary draft publication is already authorized; publication does not authorize approval or merge.

The old main runner can execute the platform program checked out from a PR, but it cannot authenticate the newly introduced intake artifact or execute the new governance workflow under its required workflow claim. No Make or PR-head helper runs before that old runner obtains AWS credentials. Consequently, the new controller cannot prove its own live authorization and governance behavior before it is installed on main.

There is a separate, confirmed deployment-rule blocker. The five inspected old-main environment Deployment records bind the dispatch event's main SHA `d3015695def1c704b1265db347ed411690314b32`, even when a job checks out a different PR head. Successful execution of that checkout does not change the record's SHA. These records cannot be presented as same-head PR Deployment records.

A PR-head Make change cannot repair trusted publication by itself. The old apply jobs grant their token only `contents: read` and `id-token: write`, and do not grant `deployments: write`. Make also executes PR-controlled code. A JSON receipt or assertion produced there is supporting input, not an independently authenticated success decision. Do not expose App credentials to that code or change a main-SHA record's claimed provenance.

A truthful PR-SHA deployment publication would need a separately trusted publisher that independently verifies an actual successful comment-triggered run, exact PR/head and current head, protected environment approvals, every required apply/drift job, target account/backend, and plan identity before creating a record explicitly linked to that run. It must reject skipped, stale, ambiguous or failed executions. An operator apply receipt cannot satisfy this contract. No such executable publisher exists in the inspected old-main lane, and this candidate does not install one before merge. A proposed external publisher would require its own concrete security review and authorization boundaries; it is not assumed available here.

A staged-acceptance decision would also be required to defer new-controller live QA to a follow-up, but that decision alone cannot satisfy the existing same-head deployment rules. This remains a reviewable local candidate, not a demonstrated executable installation path under all current conditions. Do not label deferred tests PASS or propose old-main execution alone as sufficient to merge.

Preserve all 24 current required checks, three independent eligible reviews, resolved discussions, last-push/stale-review requirements, and test/prod deployment rules. The control helper retains its eventual 25-context contract; the installation evidence invocation uses its exact fixed 24-context list until the trusted App publisher is installed and independently verified. No automatic rule relaxation or arbitrary context allowlist is introduced.

## Required execution and evidence before installation

1. Freeze the complete source, including reviewed encrypted-provider continuity. Necessary draft publication is already authorized and is handled by the parent task. Obtain fresh hosted quality/security checks and substantive reviews for that SHA; source PR192 receipts do not transfer.
2. Confirm the operator and checkpoint ownership migration for each actual account. Run reviewed platform previews against both authoritative migrated checkpoints. Local tests do not prove a zero-change live plan. Unmigrated prod is an execution blocker.
3. Resolve the trusted PR-SHA publication dependency without relaxing the deployment rules. Validate canonical backend, account, role and KMS configuration. Genuine comment-triggered platform test/prod saved-plan executions and drift verification must then have independently verified same-head Deployment records, protected environments and actual human approvals. Until that publication path exists, this step is blocked.
4. Record scoped BMAD/manual results honestly. Require an explicit staged-acceptance decision before any approval that defers new-controller live behavior; this does not waive any required check, review or deployment rule.
5. After normal installation, use the trusted controller for the follow-up's real governance/service routing and positive/negative authorization tests. Re-run final checks, deployment proof and full BMAD/manual acceptance. Activate the 25th required context only when its real App-issued proof is available.

CodeRabbit currently refuses the larger PR192 because its file count exceeds 100 and reports unavailable review capacity. A green skipped bot status is not substantive review. This candidate's exact file count is recorded in its external review manifest; required security closure is not removed to fit a bot limit.

## Test partition

All pre-existing main test responsibilities remain enabled. The three former auto-recovery tests are replaced by stronger assertions that lock conflicts and plan decryption failure cannot trigger cancellation or unsaved apply. The multi-account workflow contract now validates the actual CI-reader cutover. Tests exclusively for absent new governance resources, service templates, and alert features remain in the follow-up source and are enumerated separately; they are not silently skipped in this installation.

The installed semantic mutation gate covers immutable identities, account validation, comment/evidence authorization and promotion predicates. Operators specific to the absent governance backend are deferred with that implementation. The existing component mutation gate and 100% line/branch thresholds remain unchanged.

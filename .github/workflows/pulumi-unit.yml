name: Pulumi Unit Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  unit:
    name: Unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run unit suite
        run: make test-unit

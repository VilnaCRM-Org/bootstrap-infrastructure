name: Reviewed Source Signal
on:
  pull_request_review:
    types: [submitted, dismissed, edited]
permissions:
  contents: read
jobs:
  signal:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo 'Source admission is evaluated by the trusted main workflow.'

name: Infrastructure Promotion Scope

on:
  pull_request_target:
    branches: [main]
    types: [opened, synchronize, reopened, edited]

concurrency:
  group: promotion-status-${{ github.event.pull_request.number }}
  cancel-in-progress: false

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  scope:
    name: Report Infrastructure Promotion Scope
    environment: governance-evidence
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # Only default-branch code executes with the status-writing token.
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Verify main-only App key boundary
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 -I scripts/deployment_promotion_scope.py verify-environment
      - name: Create dedicated promotion App token
        id: promotion_app
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349
        with:
          app-id: 4840884
          owner: VilnaCRM-Org
          repositories: bootstrap-infrastructure
          private-key: ${{ secrets.GOVERNANCE_PROMOTION_APP_PRIVATE_KEY }}
          permission-statuses: write
          permission-deployments: read
          permission-actions: read
          permission-contents: read
          permission-pull-requests: read
      - name: Report current infrastructure promotion
        env:
          GH_TOKEN: ${{ steps.promotion_app.outputs.token }}
        run: python3 -I scripts/deployment_promotion_scope.py scope

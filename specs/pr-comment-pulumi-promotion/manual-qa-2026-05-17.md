# Manual QA 2026-05-17

This branch exists to exercise the PR-comment Pulumi promotion workflow after the
workflow files are available on the default branch.

The QA sequence is:

1. Open a same-repository pull request from this branch.
2. Comment `/pulumi test plan`.
3. After it succeeds, comment `/pulumi test up`.
4. After the test apply and drift check succeed, comment `/pulumi prod plan`.
5. After production preview gates succeed, comment `/pulumi prod up`.

The file intentionally does not change Pulumi resources; the QA run validates the
comment parser, trusted dispatch, exact SHA checkout, OIDC-bound preview/apply
jobs, saved-plan usage, drift checks, and production environment approval path.

## Live QA Attempt 2

This update creates a fresh same-repository pull request after the issue-comment
posting fix is present on `main`.

## Live QA Attempt 3

This update creates a fresh same-repository pull request after the scoped PR-comment permission fix is present on `main`.

### Evidence

Pull request: <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/pull/47>

Latest completed command head before review-cleanup changes:
`438a7eb705bc4072b00ff25880c11a274ebf96f8`

Current PR head is tracked by the pull request metadata and by the Pulumi PR
command result comments, rather than as a self-referential value in this file.

| Command | Intake run | Runner run | Result |
| --- | --- | --- | --- |
| `/pulumi test plan` | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/25998204756> | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/25998207958> | Passed preflight, test preview, destructive diff gate, and IAM validation. Test apply, drift, and production stages were skipped as expected. |
| `/pulumi test up` | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/25998312852> | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/25998316797> | Passed preflight, test preview, destructive diff gate, IAM validation, test apply, and post-apply drift. Production stages were skipped as expected. |
| `/pulumi prod plan` | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/25998472399> | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/25998477465> | Passed preflight, test preview, destructive diff gate, IAM validation, test apply, and post-apply drift for the same SHA. Production preview then failed before credentials were requested because `prod-preview` has no required environment variables configured. |
| `/pulumi test up` | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/26014485026> | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/26014457345> | Passed preflight, test preview, destructive diff gate, IAM validation, test apply, and post-apply drift for `438a7eb705bc4072b00ff25880c11a274ebf96f8`. |
| `/pulumi prod plan` | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/26014682262> | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/26014686651> | Passed preflight, test preview, destructive diff gate, IAM validation, test apply, test post-apply drift, production preview, production destructive diff, and production IAM validation for `438a7eb705bc4072b00ff25880c11a274ebf96f8`. |
| `/pulumi prod up` | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/26015194911> | <https://github.com/VilnaCRM-Org/bootstrap-infrastructure/actions/runs/26015299077> | Passed every stage for `438a7eb705bc4072b00ff25880c11a274ebf96f8`, including production apply and production post-apply drift after protected `prod` environment approval. |

The production blocker is external GitHub environment configuration, not a
workflow sequencing bug. `prod-preview` and `prod` exist, but neither exposes
the required variables or secrets at the time of this QA run. The production
preview prerequisite step failed before `configure-aws-credentials`, before AWS
OIDC role assumption, and before any production Pulumi command.

The blocker was later cleared by configuring the required environments and
rerunning the PR-comment production sequence. The final `/pulumi prod up` runner
completed production apply and production post-apply drift successfully.

Missing `prod-preview` values observed in runner `25998477465`:

- `AWS_ACCOUNT_ID`
- `AWS_PREVIEW_ROLE_ARN`
- `AWS_DRIFT_ROLE_ARN`
- `PULUMI_BACKEND_URL`
- `PULUMI_SECRETS_PROVIDER`
- `PULUMI_PREVIEW_STACKS`
- `PULUMI_DRIFT_STACKS`

`/pulumi prod up` was not run because production preview did not complete. The
next QA step is to configure the `prod-preview` and `prod` GitHub environments
with account-local values, rerun `/pulumi prod plan`, then run `/pulumi prod up`
after the protected `prod` environment approval.
